import { notFound } from 'next/navigation'

/**
 * Ruta desactivada temporalmente. Contenido conservado en
 * `@/components/site/pages/BrandingPageContent` — importar y exportar como default para reactivar.
 */
export default function BrandingPage() {
  notFound()
}
