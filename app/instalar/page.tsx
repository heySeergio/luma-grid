import { notFound } from 'next/navigation'

/**
 * Ruta desactivada temporalmente. Contenido y metadatos en
 * `@/components/site/pages/InstalarPageContent` y `INSTALAR_PAGE_METADATA` — restaurar import para reactivar.
 */
export default function InstalarPage() {
  notFound()
}
