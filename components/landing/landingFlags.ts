/**
 * Bloque de tarifas en la landing (`components/landing/PricingEntrance.tsx`, sección `#planes`).
 * Pon `true` para volver a mostrar la sección y el enlace «Planes» en `SiteHeader`.
 */
export const SHOW_LANDING_PRICING_SECTION = false;
